### `r title` {-}

#### Descriptive statistics {-}

<div class="hscroll-plot">
<div class="plot-container">
```{r}
plotBoxPlot(get(data)[[var]], var)
```

</div>
<div class="plot-container">
```{r}
plotHist(get(data)[[var]], var)
```
</div>
</div>

#### Distribution Fitting {-}

```{r results='asis', warning=FALSE}
if(var %in% c("Outstate", "Books", "Terminal", "Grad.Rate")) {
  #child_env <- new.env(parent = globalenv())
  #child_env$data <- College
  #child_env$var <- var
  cat(knitr::knit_child(here::here("Rmd","mixModel.Rmd"), quiet = TRUE))
  #cat(knitr::knit_child("mixModel.Rmd", envir = child_env, quiet = TRUE))
} else {
  cat(knitr::knit_child(here::here("Rmd", "singleModel.Rmd"), quiet = TRUE))
}
```